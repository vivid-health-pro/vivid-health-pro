/**
 * Return the display name for a language identifier.
 */
export declare function languageDisplayName(language: string): string;
/**
 * Guess the CDK app language based on the files in the given directory.
 *
 * Returns `undefined` if our guess fails.
 */
export declare function guessLanguage(dir: string): Promise<string | undefined>;
//# sourceMappingURL=guess-language.d.ts.map